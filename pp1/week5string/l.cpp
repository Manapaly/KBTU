#include <iostream>
#include <bits/stdc++.h>
#include <algorithm>

using namespace std;

int main(){
    string str;
    cin>>s;
    string x;
    x=sort(0, str.size()-1);
    if(str==x) cout<<"YES";
    else cout<<"NO";

    return 0;
}