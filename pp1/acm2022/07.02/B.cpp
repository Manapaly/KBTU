#include <bits/stdc++.h>

using namespace std;

int main(){
    int n;
    cin>>n;
    for(int k=0;k<n;k++){
        string s;
        cin>>s;

        for(int i=0;i<s.size();i++){
            if(s[i]=='(') int k = i;
            if(s[i]==')') int w = i;
            
        }
    }
}